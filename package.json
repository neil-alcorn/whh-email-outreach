{
  "name": "whh-email-outreach",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local WHH email outreach connector and workflow tools.",
  "scripts": {
    "build": "npm run check",
    "test": "node --test",
    "check": "node --check src/cli.js && node --check src/config.js && node --check src/data/importer.js && node --check src/data/outreach-log.js && node --check src/mail/imap.js && node --check src/mail/smtp.js && node --check src/survey/schema.js && node --check netlify/functions/survey-submit.js && node --check scripts/preview-static.js",
    "preview:survey": "node scripts/preview-static.js",
    "cli": "node src/cli.js",
    "deploy:netlify": "powershell -ExecutionPolicy Bypass -File scripts/deploy-netlify.ps1",
    "verify:netlify": "powershell -ExecutionPolicy Bypass -File scripts/deploy-netlify.ps1 -VerifyOnly"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "@netlify/database": "^1.0.0",
    "dotenv": "^16.4.7",
    "imapflow": "^1.0.179",
    "nodemailer": "^8.0.11"
  }
}
